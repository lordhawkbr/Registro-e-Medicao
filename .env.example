# Banco principal desta aplicacao (ESCALAMEDICA, REGISTROACESSO, USUARIOPERFIL etc.)
DB_USER=sa
DB_PASSWORD=changeme
DB_SERVER=localhost
DB_DATABASE=EscalaMedica

# Banco do Fluig, onde vive FDN_USERTENANT (login administrativo).
# Se nao definir usuario/senha/servidor do Fluig, cai nos valores do banco principal acima -
# so o nome do banco (FLUIG_DB_DATABASE) e obrigatorio.
FLUIG_DB_SERVER=localhost
FLUIG_DB_USER=sa
FLUIG_DB_PASSWORD=changeme
FLUIG_DB_DATABASE=fluig

PORT=3000
